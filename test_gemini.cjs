const { GoogleGenAI } = require('@google/genai');

async function test() {
  const ai = new GoogleGenAI({ apiKey: 'mock_key' }); // I will not use a real key, I just want to see if the model is correct? No, I don't have the user's key.
}
